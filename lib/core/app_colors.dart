import 'package:flutter/material.dart';

class AppColors {
  // Colores
  static const Color darkBg = Color(0xFF3B444C); // fondo principal oscuro
  static const Color lightBg = Color(0xFFE5E5E5); // blanco

  // Colores complementarios
  static const Color primary = Color(0xFF1E88E5);
  static const Color card = Color(0xFF46505A);
  static const Color textOnDark = Colors.white;
  static const Color textOnLight = Color(0xFF263238);

  // Sombras
  static const Color overlay = Color.fromRGBO(0, 0, 0, 0.25);
}
